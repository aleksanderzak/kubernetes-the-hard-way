Use TF_IN_AUTOMATION variable (see terraform article 'running terraform in automation for details')

Use remote state

Run terraform in a docker container 

talk: scalling with terraform startup enterprise